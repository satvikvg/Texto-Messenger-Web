export function getLastSeenFormat(date: Date): string {
  // TODO: Format date for last seen as below example.
  let lastSeen = "Last seen todat at 9:02 PM";
  return lastSeen;
}

export function getMessageTime(date: Date): string {
  // TODO: Format date as below example.
  let messageTime = "09:20 AM";
  return messageTime;
}

export function getMessagesDate(date: Date): string {
  // TODO: Format date as below example.
  let messagesDate = "20 JUNE 2021";
  return messagesDate;
}
